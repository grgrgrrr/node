/**
 * Created by ryndeeva_aa on 21.03.2016.
 */
//var log = require('logger')(module);

module.exports = function(module) //factory function get logged module name
{
    return function(/*...*/)
    {
        var args = [module.filename].concat([].slice.call(arguments)); // [].slice.call(arguments) ??
        console.log.apply(console, args);
    }
}