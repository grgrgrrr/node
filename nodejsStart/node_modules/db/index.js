var phrases;
exports.connect = function()
{
    phrases = require("./ru");
};

exports.getPhrase = function(name)
{
    if (!phrases[name])
    {
        throw new Error("Such phrase does not exist + ", phrases[name]);
    }
    return phrases[name];
};